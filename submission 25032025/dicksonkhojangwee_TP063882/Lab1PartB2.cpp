#include <iostream>
#include <cstdlib>
#include <ctime>
using namespace std;

int main(){ 
    srand(time(0));
    int lower,upper,secret,user;
    cout << "Guess the number game started now!" << endl;
    cout << "Enter the lower bound: ";
    cin >> lower;
    cout << "Enther the upper bound: ";
    cin >> upper;


}